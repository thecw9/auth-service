version: '3'

services:
  db-service:
    image: postgres:12
    container_name: postgres_db
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - db-service
    ports:
      - "8001:8000"

volumes:
  postgres_data:
